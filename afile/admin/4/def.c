

int* twoSum(int* numbers, int numbersSize, int target, int* returnSize) <EXACT_PARAM_NAME> {

    while (---) {
        if (---) {

        }
        <forbidden>{
            for(---;---;---){
            }

            while(---){
            }
        }
    }
}

int main()
{
    int* result;
    int i;

    int* result = twoSum(---);
    for (i = 0; ---; i++)
    {
        printf("%d", result[i]);
    }

    free(result);
    return 0;
}